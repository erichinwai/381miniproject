<!DOCTYPE html>
<html>

<head>
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <style>
      img {
         width: 100%;
         height: auto;
      }
   </style>
</head>
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">

<body>
   <div class="w3-container w3-green">
      <center>
         <h1>
            <%= p.name %>
         </h1>
      </center>
      <img src="data:<%= p.mimetype %>;base64, <%= p.image %>">
   </div>
    <div class="w3-container">
        <h2>EXIF Data</h2>
        <% if ('exif' in p && Object.keys(p.exif).length !== 0) { %>
        <h3>Borough
            <%= p.borough%>
        </h3>
        <h3>Cuisine
            <%= p.cuisine%>
        </h3>
        <h3>Street
            <%= p.street%>
        </h3>
        <h3>Building
            <%= p.address.building%>
        </h3>
        <h3>Zipcode
            <%= p.address.zipcode%>
        </h3>
        
        <h3>Rating
            <% for (i in p.grades.user.length){ %>
                  <p><%i%>. + <%=p.grades.score%> +(+ <%=p.grades.user%>+)</p>
          <%}%>
            
        </h3>
        <h3>Created by
            <%= p.owner%>
        </h3>
        <h3>Upload Date/Time:
            <%= p.exif.exif.DateTimeOriginal %>
        </h3>
        <% if (lat != -1 && lon != -1) { %>
        <h3>GPS:
            <a href='/map?lat=<%=lat%>&lon=<%=lon%>&p.name=<%=p.name%>'>
                <%=lon%>, <%=lat%>
            </a>
        </h3>
        <% } %>
        <% } else { %>
        <h3>none</h3>
        <% } %>
    </div>
</html>